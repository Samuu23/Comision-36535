{
  "name": "cliente_knex",
  "version": "1.0.0",
  "description": "1. Creamos un proyecto Node.js con npm init -y 2. Instalamos la dependencias Knex y mysql con npm i knex mysql (mysql es el plugin necesario para trabajar con MariaDB)  3. Levantamos el motor de base de datos MariaDB con XAMPP. 4. Creamos los archivos necesarios para probar los comandos SQL necesarios en acciones CRUD.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^2.1.0",
    "mysql": "^2.18.1"
  }
}
